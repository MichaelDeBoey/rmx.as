{
  "name": "rmx.as",
  "type": "module",
  "description": "Personal URL shortener powered by Cloudflare Workers.",
  "engines": {
    "node": "24"
  },
  "scripts": {
    "format": "node scripts/format.ts",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^24",
    "wrangler": "^4.64.0"
  }
}
